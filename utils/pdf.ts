import{jsPDF}from'jspdf';import{Filesystem,Directory}from'@capacitor/filesystem';import{Share}from'@capacitor/share';import{Capacitor}from'@capacitor/core';import{Order,Prices,User}from'../types';import{EmployeeAdjustmentForReport}from'../services/employeeAdjustments';const fallbackNames:Record<string,string>={morningTea:'Morning Tea',lunchMeals:'Meals',lunchEgg:'Egg',lunchFishMeat:'Fish/Meat',eveningTea:'Evening Tea'};export function orderTotal(order:Order,prices:Prices){return Object.keys(order.items).reduce((s,k)=>order.items[k]?s+Number(order.itemPrices?.[k]??(k in prices?prices[k as keyof Prices]:0)):s,0)}export function orderText(order:Order){return Object.keys(order.items).filter(k=>order.items[k]).map(k=>order.itemNames?.[k]||fallbackNames[k]||k).join(', ')||'No items'}function wrap(doc:jsPDF,text:string,x:number,y:number,w:number){const lines=doc.splitTextToSize(text,w)as string[];doc.text(lines,x,y);return y+lines.length*5}function page(doc:jsPDF,y:number,needed=10){if(y+needed>282){doc.addPage();return 18}return y}async function deliverPdf(doc:jsPDF,filename:string){const blob=doc.output('blob');if(Capacitor.isNativePlatform()){const base64=await new Promise<string>((resolve,reject)=>{const reader=new FileReader();reader.onloadend=()=>resolve(String(reader.result).split(',')[1]||'');reader.onerror=reject;reader.readAsDataURL(blob)});await Filesystem.writeFile({path:filename,data:base64,directory:Directory.Cache,recursive:true});const uri=await Filesystem.getUri({path:filename,directory:Directory.Cache});try{await Share.share({title:filename,text:'GoCanteen PDF report',url:uri.uri,dialogTitle:'View or send PDF'});}finally{try{await Filesystem.deleteFile({path:filename,directory:Directory.Cache})}catch{}}return}const url=URL.createObjectURL(blob);const a=document.createElement('a');a.href=url;a.download=filename;document.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(url),30000)}
export async function downloadDailyPdf(date:string,users:User[],orders:Order[],prices:Prices,adjustments:EmployeeAdjustmentForReport[]=[]){const doc=new jsPDF();const day=orders.filter(o=>o.date===date);const extras=adjustments.filter(a=>a.adjustment_date===date);const extraByEmployee=new Map<string,number>();extras.forEach(a=>extraByEmployee.set(a.employeeCode,(extraByEmployee.get(a.employeeCode)||0)+Number(a.amount||0)));let total=day.reduce((s,o)=>s+orderTotal(o,prices),0)+extras.reduce((s,a)=>s+Number(a.amount||0),0);doc.setFontSize(18);doc.text('GoCanteen - Daily Order Report',14,18);doc.setFontSize(10);doc.text(`Date: ${date}`,14,26);doc.text(`Employees / Orders: ${day.length}`,14,36);doc.setFontSize(12);doc.text(`Total Collection: Rs.${total.toFixed(2)}`,14,44);let y=54;const employeeCodes=[...new Set([...day.map(o=>o.employeeId),...extras.map(a=>a.employeeCode)])];employeeCodes.forEach((code,i)=>{const o=day.find(x=>x.employeeId===code);const u=users.find(x=>x.id===code);const orderAmount=o?orderTotal(o,prices):0;const extra=extraByEmployee.get(code)||0;y=page(doc,y,28);doc.setFont('helvetica','bold');y=wrap(doc,`${i+1}. ${u?.name||code} | SR: ${code}`,14,y,182);doc.setFont('helvetica','normal');if(o){y=wrap(doc,`Items: ${orderText(o)}`,18,y+1,178);doc.text(`Order Amount: Rs.${orderAmount.toFixed(2)}`,18,y+1);y+=7}if(extra){const desc=extras.filter(a=>a.employeeCode===code).map(a=>a.description||'Additional amount').join(', ');y=wrap(doc,`Admin Added: ${desc}`,18,y+1,178);doc.text(`Additional Amount: Rs.${extra.toFixed(2)}`,18,y+1);y+=7}doc.setFont('helvetica','bold');doc.text(`Employee Total: Rs.${(orderAmount+extra).toFixed(2)}`,18,y+1);doc.setFont('helvetica','normal');y+=8});await deliverPdf(doc,`GoCanteen-Daily-${date}.pdf`)}
export async function downloadMonthlyPdf(month:number,year:number,users:User[],orders:Order[],prices:Prices,adjustments:EmployeeAdjustmentForReport[]=[]){const doc=new jsPDF();const monthName=new Date(year,month,1).toLocaleString('en-IN',{month:'long'});const employees=users.filter(u=>u.role==='employee');const monthAdjustments=adjustments.filter(a=>{const d=new Date(`${a.adjustment_date}T00:00:00`);return d.getMonth()===month&&d.getFullYear()===year});let y=36,grand=0;doc.setFontSize(18);doc.text('GoCanteen - Monthly Employee Report',14,18);doc.setFontSize(10);doc.text(`${monthName} ${year}`,14,26);employees.forEach((u,i)=>{const mine=orders.filter(o=>o.employeeId===u.id&&(()=>{const d=new Date(`${o.date}T00:00:00`);return d.getMonth()===month&&d.getFullYear()===year})());const orderTotalAmount=mine.reduce((s,o)=>s+orderTotal(o,prices),0);const extra=monthAdjustments.filter(a=>a.employeeCode===u.id).reduce((s,a)=>s+Number(a.amount||0),0);const total=orderTotalAmount+extra;grand+=total;y=page(doc,y,25);doc.setFont('helvetica','bold');y=wrap(doc,`${i+1}. ${u.name||'-'}`,14,y,182);doc.setFont('helvetica','normal');y=wrap(doc,`SR Number: ${u.id}   Mobile: ${u.mobile||'-'}`,14,y+1,182);doc.text(`Orders: ${mine.length}   Food Amount: Rs.${orderTotalAmount.toFixed(2)}   Admin Added: Rs.${extra.toFixed(2)}`,14,y+1);y+=9});y=page(doc,y,20);doc.setFontSize(13);doc.setFont('helvetica','bold');doc.text(`Monthly Total: Rs.${grand.toFixed(2)}`,14,y+5);await deliverPdf(doc,`GoCanteen-Monthly-${year}-${String(month+1).padStart(2,'0')}.pdf`)}
export async function downloadRevenuePdf(month:number,year:number,orders:Order[],prices:Prices,expenses:number,adjustments:EmployeeAdjustmentForReport[]=[]){const doc=new jsPDF();const monthName=new Date(year,month,1).toLocaleString('en-IN',{month:'long'});const start=`${year}-${String(month+1).padStart(2,'0')}-01`;const end=new Date(year,month+1,0).toISOString().slice(0,10);const collection=orders.filter(o=>o.date>=start&&o.date<=end).reduce((s,o)=>s+orderTotal(o,prices),0)+adjustments.filter(a=>a.adjustment_date>=start&&a.adjustment_date<=end).reduce((s,a)=>s+Number(a.amount||0),0);const revenue=collection-expenses;doc.setFontSize(18);doc.text('GoCanteen - Revenue Report',14,18);doc.setFontSize(11);doc.text(`${monthName} ${year}`,14,28);doc.text(`Period: ${start} to ${end}`,14,36);doc.setFontSize(13);doc.text(`Total Collection: Rs.${collection.toFixed(2)}`,14,52);doc.text(`Total Expenses: Rs.${expenses.toFixed(2)}`,14,62);doc.setFont('helvetica','bold');doc.text(`Net Revenue: Rs.${revenue.toFixed(2)}`,14,74);doc.setFont('helvetica','normal');doc.setFontSize(9);doc.text('Net Revenue = Total Collection - Total Expenses',14,84);await deliverPdf(doc,`GoCanteen-Revenue-${year}-${String(month+1).padStart(2,'0')}.pdf`)}
