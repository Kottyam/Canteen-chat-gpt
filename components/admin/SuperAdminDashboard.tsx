import React,{useCallback,useEffect,useMemo,useState}from'react';
import CanteenHeader from'../shared/CanteenHeader';
import ChangePasswordModal from'../auth/ChangePasswordModal';
import{useAuth}from'../../context/AuthContext';
import{createPlan,loadPlatformData,reviewSubscriptionPayment,setSubscription,updatePlan,setDefaultPlan,deletePlan,SubscriptionPlan,SubscriptionPayment,CanteenSubscription}from'../../services/subscriptionPlatform';

type Tab='dashboard'|'canteens'|'subscriptions'|'payments'|'admins'|'plans'|'settings';
const money=(n:number,c='INR')=>new Intl.NumberFormat('en-IN',{style:'currency',currency:c,maximumFractionDigits:2}).format(n||0);
const date=(v:string|null)=>v?new Date(v).toLocaleDateString('en-IN',{day:'2-digit',month:'short',year:'numeric'}):'—';
const badge=(s:string)=>{const m:Record<string,string>={active:'bg-green-100 text-green-700',trial:'bg-blue-100 text-blue-700',payment_pending:'bg-amber-100 text-amber-700',expired:'bg-red-100 text-red-700',suspended:'bg-gray-200 text-gray-700',paid:'bg-green-100 text-green-700',pending:'bg-amber-100 text-amber-700',failed:'bg-red-100 text-red-700',refunded:'bg-purple-100 text-purple-700'};return <span className={`rounded-full px-2.5 py-1 text-xs font-bold ${m[s]||'bg-gray-100 text-gray-700'}`}>{s.replace('_',' ').toUpperCase()}</span>};

const SuperAdminDashboard:React.FC=()=>{const{user}=useAuth();const[tab,setTab]=useState<Tab>('dashboard');const[data,setData]=useState<any>(null);const[busy,setBusy]=useState(false);const[message,setMessage]=useState('');const[passwordOpen,setPasswordOpen]=useState(false);const[selectedCanteen,setSelectedCanteen]=useState<string|null>(null);
const refresh=useCallback(async()=>{setBusy(true);setMessage('');try{setData(await loadPlatformData())}catch(e:any){setMessage(e?.message||'Could not load platform data.')}finally{setBusy(false)}},[]);
useEffect(()=>{void refresh()},[refresh]);
if(user?.adminRole!=='super_admin')return null;
const plans:SubscriptionPlan[]=data?.plans||[];const subs:CanteenSubscription[]=data?.subscriptions||[];const payments:SubscriptionPayment[]=data?.payments||[];const canteens=data?.canteens||[];const profiles=data?.profiles||[];
const ownerByCanteen=new Map(profiles.filter((p:any)=>p.role==='admin'&&p.admin_role==='owner').map((p:any)=>[p.canteen_id,p]));
const subByCanteen=new Map(subs.map(s=>[s.canteen_id,s]));const planById=new Map(plans.map(p=>[p.id,p]));
const admins=profiles.filter((p:any)=>p.role==='admin'&&p.admin_role!=='super_admin');
const employeeCount=(id:string)=>profiles.filter((p:any)=>p.canteen_id===id&&p.role==='employee').length;
const runAction=async(fn:()=>Promise<unknown>)=>{setBusy(true);setMessage('');try{await fn();await refresh();}catch(e:any){setMessage(e?.message||'Action failed.')}finally{setBusy(false)}};
const statusAction=async(c:string,a:string,p?:string,d?:number)=>runAction(()=>setSubscription(c,a,p,d));
const selected=selectedCanteen?canteens.find((c:any)=>c.id===selectedCanteen):null;const selectedSub=selectedCanteen?subByCanteen.get(selectedCanteen):null;
const selectedAdmins=selectedCanteen?admins.filter((p:any)=>p.canteen_id===selectedCanteen):[];const selectedPayments=selectedSub?payments.filter(p=>p.subscription_id===selectedSub.id):[];
const nav=[['dashboard','Dashboard'],['canteens','Canteens'],['subscriptions','Subscriptions'],['payments','Payments'],['admins','Admins'],['plans','Plans'],['settings','Settings']] as const;
return <div className="min-h-screen w-full bg-gray-100 pb-14"><CanteenHeader onChangePassword={()=>setPasswordOpen(true)}/><main className="mx-auto w-full max-w-7xl px-2 py-3 sm:px-5 sm:py-5"><div className="mb-3 rounded-xl bg-white p-2 shadow-sm"><div className="grid grid-cols-2 gap-2 sm:grid-cols-4 lg:grid-cols-7">{nav.map(([k,l])=><button key={k} onClick={()=>setTab(k)} className={`min-h-11 rounded-lg px-2 text-sm font-bold ${tab===k?'bg-primary-600 text-white':'bg-gray-100 text-gray-700'}`}>{l}</button>)}</div></div>{message&&<div className="mb-3 rounded-lg border border-red-200 bg-red-50 p-3 text-sm font-semibold text-red-700">{message}</div>}{busy&&<div className="mb-3 rounded-lg bg-blue-50 p-2 text-center text-xs font-semibold text-blue-700">Updating platform data…</div>}
{tab==='dashboard'&&<section className="space-y-4"><div><h2 className="text-2xl font-extrabold text-gray-900">Platform Dashboard</h2><p className="text-sm text-gray-500">GoCanteen platform-level administration</p></div><div className="grid grid-cols-2 gap-3 sm:grid-cols-4">{[['Total Canteens',data?.stats?.total_canteens||0],['Active',data?.stats?.active_canteens||0],['Trial',data?.stats?.trial_canteens||0],['Payment Pending',data?.stats?.payment_pending||0],['Expired',data?.stats?.expired||0],['Suspended',data?.stats?.suspended||0],['Employees',data?.stats?.total_employees||0],['Admins',data?.stats?.total_admins||0]].map(([l,v])=><div key={String(l)} className="rounded-xl border bg-white p-4 shadow-sm"><p className="text-xs font-bold uppercase text-gray-500">{l}</p><p className="mt-1 text-2xl font-extrabold text-primary-700">{v}</p></div>)}</div></section>}
{tab==='canteens'&&<section className="space-y-4"><h2 className="text-2xl font-extrabold">Canteens</h2>{selected?<div className="rounded-xl bg-white p-4 shadow-sm"><button onClick={()=>setSelectedCanteen(null)} className="mb-4 text-sm font-bold text-primary-700">← Back to Canteens</button><h3 className="text-xl font-extrabold">{selected.name}</h3><p className="mt-1 text-xs text-gray-500">{selected.id}</p><div className="mt-4 grid grid-cols-2 gap-3 sm:grid-cols-4"><div><b>Owner</b><p>{ownerByCanteen.get(selected.id)?.full_name||'—'}</p></div><div><b>Employees</b><p>{employeeCount(selected.id)}</p></div><div><b>Plan</b><p>{selectedSub?planById.get(selectedSub.plan_id)?.name||'—':'—'}</p></div><div><b>Status</b><p>{selectedSub?badge(selectedSub.status):'NOT ASSIGNED'}</p></div></div><div className="mt-5 flex flex-wrap gap-2">{selectedSub&&<><button onClick={()=>void statusAction(selected.id,'suspend')} className="rounded-lg bg-gray-800 px-3 py-2 text-sm font-bold text-white">Suspend</button><button onClick={()=>void statusAction(selected.id,'payment_pending')} className="rounded-lg bg-amber-600 px-3 py-2 text-sm font-bold text-white">Payment Pending</button><button onClick={()=>void statusAction(selected.id,'extend_trial',selectedSub.plan_id,7)} className="rounded-lg bg-blue-600 px-3 py-2 text-sm font-bold text-white">Extend Trial +7d</button></>}</div><div className="mt-5"><h4 className="font-extrabold">Administrators</h4><div className="mt-2 space-y-2">{selectedAdmins.map((p:any)=><div key={p.id} className="rounded-lg border p-3"><b>{p.full_name}</b><span className="ml-2 text-xs font-bold uppercase text-gray-500">{p.admin_role}</span><p className="text-xs text-gray-500">{p.status}</p></div>)}</div></div><div className="mt-5"><h4 className="font-extrabold">Payment History</h4><div className="mt-2 space-y-2">{selectedPayments.length?selectedPayments.map(p=><div key={p.id} className="flex flex-wrap justify-between gap-2 rounded-lg border p-3"><span>{money(p.amount,p.currency)} · {p.transaction_reference||'No reference'}</span><span>{badge(p.payment_status)} {date(p.payment_date)}</span></div>):<p className="text-sm text-gray-500">No subscription payments.</p>}</div></div></div>:<div className="overflow-x-auto rounded-xl bg-white shadow-sm"><table className="w-full min-w-[800px] text-sm"><thead className="bg-gray-50 text-left"><tr>{['Canteen','Owner','Employees','Plan','Status','Expiry','Action'].map(x=><th key={x} className="p-3">{x}</th>)}</tr></thead><tbody>{canteens.map((c:any)=>{const s=subByCanteen.get(c.id);return <tr key={c.id} className="border-t"><td className="p-3 font-bold">{c.name}</td><td className="p-3">{ownerByCanteen.get(c.id)?.full_name||'—'}</td><td className="p-3">{employeeCount(c.id)}</td><td className="p-3">{s?planById.get(s.plan_id)?.name||'—':'—'}</td><td className="p-3">{s?badge(s.status):'—'}</td><td className="p-3">{date(s?.subscription_end||s?.trial_end||null)}</td><td className="p-3"><button onClick={()=>setSelectedCanteen(c.id)} className="rounded-lg bg-primary-600 px-3 py-2 text-xs font-bold text-white">Details</button></td></tr>})}</tbody></table></div>}</section>}
{tab==='subscriptions'&&<section className="space-y-4"><h2 className="text-2xl font-extrabold">Subscriptions</h2><div className="space-y-2">{canteens.map((c:any)=>{const s=subByCanteen.get(c.id);return <div key={c.id} className="rounded-xl bg-white p-4 shadow-sm"><div className="flex flex-wrap items-center justify-between gap-3"><div><b>{c.name}</b><p className="text-xs text-gray-500">{s?planById.get(s.plan_id)?.name||'No plan':'No subscription'}</p></div><div>{s?badge(s.status):<span className="text-sm text-gray-500">Not assigned</span>}</div></div><div className="mt-3 flex flex-wrap gap-2">{!s&&plans.filter(p=>p.active).slice(0,3).map(p=><button key={p.id} onClick={()=>void statusAction(c.id,'start_trial',p.id,p.trial_days)} disabled={!p.trial_days} className="rounded-lg bg-blue-600 px-3 py-2 text-xs font-bold text-white disabled:opacity-40">Start {p.name} Trial</button>)}{s&&<><select onChange={e=>{if(e.target.value)void statusAction(c.id,'change_plan',e.target.value)}} defaultValue="" className="rounded-lg border px-2 py-2 text-xs"><option value="">Change Plan</option>{plans.filter(p=>p.active).map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</select><button onClick={()=>void statusAction(c.id,'activate',s.plan_id)} className="rounded-lg bg-green-600 px-3 py-2 text-xs font-bold text-white">Activate</button><button onClick={()=>void statusAction(c.id,'suspend')} className="rounded-lg bg-gray-800 px-3 py-2 text-xs font-bold text-white">Suspend</button></>}</div></div>})}</div></section>}
{tab==='payments'&&<section className="space-y-4"><h2 className="text-2xl font-extrabold">Subscription Payments</h2><div className="overflow-x-auto rounded-xl bg-white shadow-sm"><table className="w-full min-w-[850px] text-sm"><thead className="bg-gray-50 text-left"><tr>{['Canteen','Plan','Amount','Status','Date','Reference','Action'].map(x=><th key={x} className="p-3">{x}</th>)}</tr></thead><tbody>{payments.map(p=>{const c=canteens.find((x:any)=>x.id===p.canteen_id);return <tr key={p.id} className="border-t"><td className="p-3 font-bold">{c?.name||p.canteen_id}</td><td className="p-3">{planById.get(p.plan_id)?.name||'—'}</td><td className="p-3">{money(p.amount,p.currency)}</td><td className="p-3">{badge(p.payment_status)}</td><td className="p-3">{date(p.payment_date)}</td><td className="p-3">{p.transaction_reference||'—'}</td><td className="p-3">{p.payment_status==='pending'?<><button onClick={()=>void runAction(()=>reviewSubscriptionPayment(p.id,'paid'))} className="rounded-lg bg-green-600 px-3 py-2 text-xs font-bold text-white">Confirm Payment</button><button onClick={()=>void runAction(()=>reviewSubscriptionPayment(p.id,'failed'))} className="rounded-lg bg-red-600 px-3 py-2 text-xs font-bold text-white">Reject Payment</button></>:<span className="text-xs font-semibold text-gray-500">Reviewed</span>}</td></tr>})}</tbody></table>{!payments.length&&<p className="p-6 text-sm text-gray-500">No subscription payments recorded yet.</p>}</div></section>}
{tab==='admins'&&<section className="space-y-4"><h2 className="text-2xl font-extrabold">Canteen Admins</h2><div className="overflow-x-auto rounded-xl bg-white shadow-sm"><table className="w-full min-w-[650px] text-sm"><thead className="bg-gray-50 text-left"><tr>{['Name','Role','Canteen','Status'].map(x=><th key={x} className="p-3">{x}</th>)}</tr></thead><tbody>{admins.map((p:any)=><tr key={p.id} className="border-t"><td className="p-3 font-bold">{p.full_name}</td><td className="p-3 uppercase">{p.admin_role}</td><td className="p-3">{canteens.find((c:any)=>c.id===p.canteen_id)?.name||'—'}</td><td className="p-3">{p.status}</td></tr>)}</tbody></table></div></section>}
{tab==='plans'&&<Plans plans={plans} subscriptions={subs} payments={payments} refresh={refresh} busy={busy}/>}
{tab==='settings'&&<section className="rounded-xl bg-white p-5 shadow-sm"><h2 className="text-2xl font-extrabold">Platform Settings</h2><p className="mt-2 text-sm text-gray-600">Subscription gateway integration is intentionally not enabled in this foundation phase.</p><p className="mt-1 text-sm text-gray-600">Subscription payment records and activation are separated from employee food-bill payments.</p></section>}
</main><ChangePasswordModal isOpen={passwordOpen} onClose={()=>setPasswordOpen(false)}/></div>};

const Plans:React.FC<{plans:SubscriptionPlan[];subscriptions:CanteenSubscription[];payments:SubscriptionPayment[];refresh:()=>Promise<void>;busy:boolean}>=({plans,subscriptions,payments,refresh,busy})=>{
  const[form,setForm]=useState({name:'',description:'',price:'',billing_period:'monthly',currency:'INR',trial_days:'30',is_default:false});
  const[editing,setEditing]=useState<string|null>(null);
  const[err,setErr]=useState('');
  const save=async()=>{
    setErr('');
    try{
      const current=editing?plans.find(x=>x.id===editing):null;
      const input={name:form.name.trim(),description:form.description||null,price:Number(form.price||0),billing_period:form.billing_period as any,currency:form.currency.toUpperCase(),trial_days:Number(form.trial_days||0),active:current?current.active:true,is_default:false};
      const saved=editing?await updatePlan(editing,input):await createPlan(input);
      if(form.is_default) await setDefaultPlan(saved.id);
      setForm({name:'',description:'',price:'',billing_period:'monthly',currency:'INR',trial_days:'30',is_default:false});setEditing(null);await refresh();
    }catch(e:any){setErr(e?.message||'Could not save plan.')}
  };
  const edit=(p:SubscriptionPlan)=>{setEditing(p.id);setForm({name:p.name,description:p.description||'',price:String(p.price),billing_period:p.billing_period,currency:p.currency,trial_days:String(p.trial_days),is_default:p.is_default})};
  const cancel=()=>{setEditing(null);setErr('');setForm({name:'',description:'',price:'',billing_period:'monthly',currency:'INR',trial_days:'30',is_default:false})};
  const remove=async(p:SubscriptionPlan)=>{
    if(!window.confirm(`Delete plan "${p.name}"? Used plans are protected.`))return;
    setErr('');
    try{await deletePlan(p.id);await refresh()}catch(e:any){setErr(e?.message||'Plan cannot be deleted. Deactivate it instead.')}
  };
  return <section className="space-y-4">
    <div><h2 className="text-2xl font-extrabold">Subscription Plans</h2><p className="text-sm text-gray-500">Create, edit, activate/deactivate and safely delete platform plans.</p></div>
    <div className="rounded-xl bg-white p-4 shadow-sm">
      <div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-3">
        <input className="rounded-lg border p-3" placeholder="Plan name" value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/>
        <input className="rounded-lg border p-3" placeholder="Description" value={form.description} onChange={e=>setForm({...form,description:e.target.value})}/>
        <input className="rounded-lg border p-3" type="number" min="0" placeholder="Price" value={form.price} onChange={e=>setForm({...form,price:e.target.value})}/>
        <select className="rounded-lg border p-3" value={form.billing_period} onChange={e=>setForm({...form,billing_period:e.target.value})}><option value="monthly">Monthly</option><option value="quarterly">Quarterly</option><option value="yearly">Yearly</option></select>
        <input className="rounded-lg border p-3" maxLength={3} value={form.currency} onChange={e=>setForm({...form,currency:e.target.value})}/>
        <input className="rounded-lg border p-3" type="number" min="1" placeholder="Trial days" value={form.trial_days} onChange={e=>setForm({...form,trial_days:e.target.value})}/>
      </div>
      <label className="mt-3 flex items-center gap-2 text-sm font-semibold"><input type="checkbox" checked={form.is_default} onChange={e=>setForm({...form,is_default:e.target.checked})}/> Default plan for automatic trials</label>
      {err&&<p className="mt-3 text-sm font-semibold text-red-600">{err}</p>}
      <div className="mt-3 flex gap-2">
        <button disabled={busy||!form.name.trim()} onClick={()=>void save()} className="rounded-lg bg-primary-600 px-4 py-2.5 font-bold text-white disabled:opacity-40">{editing?'Save Changes':'Create Plan'}</button>
        {editing&&<button onClick={cancel} className="rounded-lg bg-gray-200 px-4 py-2.5 font-bold text-gray-700">Cancel</button>}
      </div>
    </div>
    <div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-3">
      {plans.map(p=>{
        const used=subscriptions.some(s=>s.plan_id===p.id)||payments.some(x=>x.plan_id===p.id);
        return <div key={p.id} className="rounded-xl bg-white p-4 shadow-sm">
          <div className="flex items-start justify-between gap-2"><div><h3 className="font-extrabold">{p.name}</h3><div className="mt-1 flex flex-wrap gap-1">{p.active?<span className="rounded-full bg-green-100 px-2.5 py-1 text-xs font-bold text-green-700">ACTIVE</span>:<span className="rounded-full bg-gray-200 px-2.5 py-1 text-xs font-bold text-gray-700">INACTIVE</span>}{p.is_default&&<span className="rounded-full bg-blue-100 px-2.5 py-1 text-xs font-bold text-blue-700">DEFAULT</span>}{used&&<span className="rounded-full bg-gray-200 px-2.5 py-1 text-xs font-bold text-gray-700">USED / PROTECTED</span>}</div></div></div>
          <p className="mt-2 text-sm text-gray-500">{p.description||'No description'}</p>
          <p className="mt-3 text-xl font-extrabold">{money(p.price,p.currency)} <span className="text-xs font-semibold text-gray-500">/ {p.billing_period}</span></p>
          <p className="mt-1 text-xs text-gray-500">Trial: {p.trial_days} days</p>
          <div className="mt-3 flex flex-wrap gap-2">
            <button onClick={()=>edit(p)} className="rounded-lg bg-primary-600 px-3 py-2 text-xs font-bold text-white">Edit</button>
            <button onClick={()=>void (async()=>{try{await updatePlan(p.id,{active:!p.active});await refresh()}catch(e:any){setErr(e?.message||'Could not change plan status.')}})()} className={`rounded-lg px-3 py-2 text-xs font-bold ${p.active?'bg-gray-800 text-white':'bg-green-600 text-white'}`}>{p.active?'Deactivate':'Activate'}</button>
            {!p.is_default&&p.active&&<button onClick={()=>void (async()=>{try{await setDefaultPlan(p.id);await refresh()}catch(e:any){setErr(e?.message||'Could not set default plan.')}})()} className="rounded-lg bg-blue-600 px-3 py-2 text-xs font-bold text-white">Set Default</button>}
            <button disabled={used} title={used?'This plan is referenced by subscription/payment history and cannot be deleted.':'Delete unused plan'} onClick={()=>void remove(p)} className="rounded-lg bg-red-600 px-3 py-2 text-xs font-bold text-white disabled:cursor-not-allowed disabled:opacity-40">{used?'Delete Protected':'Delete'}</button>
          </div>
        </div>
      })}
    </div>
  </section>};
export default SuperAdminDashboard;
