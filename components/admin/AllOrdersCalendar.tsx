import React,{useEffect,useMemo,useState}from'react';import{useData}from'../../context/DataContext';import{getMonthName,formatDate}from'../../utils/helpers';import{Order}from'../../types';import{supabase}from'../../supabase';import{historicalMemberName}from'../../utils/memberIdentity';import{EmployeeAdjustmentForReport,loadHistoricalEmployeeAdjustments}from'../../services/employeeAdjustments';
const WEEKDAYS=[['Monday',1],['Tuesday',2],['Wednesday',3],['Thursday',4],['Friday',5],['Saturday',6],['Sunday',0]] as const;
const AllOrdersCalendar:React.FC=()=>{const{users,orders}=useData();const[currentDate,setCurrentDate]=useState(new Date());const[selectedDate,setSelectedDate]=useState<string|null>(null);const[message,setMessage]=useState('');const[saving,setSaving]=useState(false);const[recurringWeekdays,setRecurringWeekdays]=useState<number[]>([]);const[declaredDates,setDeclaredDates]=useState<string[]>([]);const[holidayLoading,setHolidayLoading]=useState(true);const[adjustments,setAdjustments]=useState<EmployeeAdjustmentForReport[]>([]);const loadHolidayConfiguration=async()=>{if(!supabase)return;setHolidayLoading(true);try{const{data,error}=await supabase.rpc('get_holiday_configuration');if(error)throw error;const cfg=(data||{}) as {recurring_weekdays?:unknown;declared_dates?:unknown};setRecurringWeekdays(Array.isArray(cfg.recurring_weekdays)?cfg.recurring_weekdays.map(Number):[]);setDeclaredDates(Array.isArray(cfg.declared_dates)?cfg.declared_dates.map(String):[])}catch(error:any){console.error('Holiday configuration load failed',error);setMessage('Could not load the latest holiday configuration.');setRecurringWeekdays([]);setDeclaredDates([])}finally{setHolidayLoading(false)}};useEffect(()=>{void loadHolidayConfiguration();const refresh=()=>void loadHolidayConfiguration();window.addEventListener('focus',refresh);document.addEventListener('visibilitychange',refresh);const timer=window.setInterval(refresh,10000);return()=>{window.removeEventListener('focus',refresh);document.removeEventListener('visibilitychange',refresh);window.clearInterval(timer)}},[]);useEffect(()=>{let alive=true;if(!selectedDate){setAdjustments([]);return()=>{alive=false}};const load=async()=>{try{const rows=await loadHistoricalEmployeeAdjustments(selectedDate,selectedDate);if(alive)setAdjustments(rows)}catch(error){console.error('Daily admin-added amounts load failed',error);if(alive)setAdjustments([])}};void load();if(!supabase)return()=>{alive=false};const channel=supabase.channel(`gocanteen-daily-adjustments-${selectedDate}`).on('postgres_changes',{event:'*',schema:'public',table:'employee_adjustments'},()=>void load()).subscribe();return()=>{alive=false;void channel.unsubscribe()}},[selectedDate]);const ordersByDate=useMemo(()=>orders.reduce((acc,order)=>{(acc[order.date]=acc[order.date]||[]).push(order);return acc},{}as Record<string,Order[]>),[orders]);const identityForOrder=(order:Order)=>order.memberIdentityId||order.employeeId;const employeeName=(order:Order)=>{const identity=identityForOrder(order);const member=users.find(u=>u.identityId===identity||u.id===identity);const snapshot=order.memberNameSnapshot?.trim();return historicalMemberName(snapshot&&snapshot.toLowerCase()!=='member'?snapshot:undefined,member?.name,member?.status)};const isHoliday=(date:Date)=>declaredDates.includes(formatDate(date))||recurringWeekdays.includes(date.getDay());const recurringLabel=useMemo(()=>WEEKDAYS.filter(([,value])=>recurringWeekdays.includes(value)).map(([label])=>label),[recurringWeekdays]);const toggleHoliday=async(dateString:string)=>{if(!supabase||holidayLoading||saving)return;const date=new Date(`${dateString}T00:00:00`);if(recurringWeekdays.includes(date.getDay())){setMessage('This date is covered by the recurring holiday setting. Change it in Settings → Time Management → Holiday Management.');return}setSaving(true);setMessage('');try{if(declaredDates.includes(dateString)){const{error}=await supabase.from('holiday_dates').delete().eq('holiday_date',dateString);if(error)throw error;setMessage(`${dateString} declared holiday removed.`)}else{const{error}=await supabase.rpc('admin_add_declared_holiday',{p_date:dateString});if(error)throw error;setMessage(`${dateString} declared as holiday.`)}await loadHolidayConfiguration()}catch(error:any){console.error(error);setMessage(error?.message||'Holiday update failed. Please try again.')}finally{setSaving(false)}};const selectedOrders=selectedDate?ordersByDate[selectedDate]||[]:[];const selectedAdjustments=selectedDate?adjustments.filter(a=>a.adjustment_date===selectedDate):[];const normalTotal=(order:Order)=>Object.keys(order.items||{}).reduce((s,c)=>s+(order.items[c]?Number(order.itemPrices?.[c]||0)*Math.max(1,Number(order.itemQuantities?.[c]||1)):0),0);const guestTotal=(order:Order)=>Object.keys(order.guestItems||{}).reduce((s,c)=>s+(order.guestItems?.[c]?Number(order.guestItemPrices?.[c]||0)*Math.max(1,Number(order.guestItemQuantities?.[c]||1)):0),0);const changeMonth=(offset:number)=>{setCurrentDate(new Date(currentDate.getFullYear(),currentDate.getMonth()+offset,1));setSelectedDate(null)};const firstDay=new Date(currentDate.getFullYear(),currentDate.getMonth(),1).getDay();const daysInMonth=new Date(currentDate.getFullYear(),currentDate.getMonth()+1,0).getDate();const days:React.ReactNode[]=[];for(let i=0;i<firstDay;i++)days.push(<div key={`blank-${i}`} className="min-h-16 border bg-gray-50 sm:min-h-20"/>);for(let day=1;day<=daysInMonth;day++){const date=new Date(currentDate.getFullYear(),currentDate.getMonth(),day);const dateString=formatDate(date);const holiday=isHoliday(date);const count=(ordersByDate[dateString]||[]).length;const today=formatDate(new Date())===dateString;days.push(<button key={day} type="button" onClick={()=>setSelectedDate(dateString)} className={`min-h-16 border p-1 text-left sm:min-h-20 ${holiday?'bg-gray-200 text-gray-500':'bg-white hover:bg-green-50'} ${today?'ring-2 ring-primary-400 ring-inset':''}`}><div className="text-center text-xs font-semibold sm:text-sm">{day}</div><div className="mt-1 text-center text-[9px] sm:text-xs">{holiday?'Holiday':count?`${count} orders`:''}</div></button>)}const selectedHoliday=selectedDate?isHoliday(new Date(`${selectedDate}T00:00:00`)):false;const selectedRecurring=selectedDate?recurringWeekdays.includes(new Date(`${selectedDate}T00:00:00`).getDay()):false;const orderIdentities=new Set(selectedOrders.map(identityForOrder));const adjustmentOnly=selectedAdjustments.filter(a=>!orderIdentities.has(a.employeeCode));return <div className="w-full min-w-0"><h3 className="mb-4 text-xl font-bold text-gray-800 sm:text-2xl">Orders &amp; Holiday Calendar</h3><div className="mb-4 rounded-lg border bg-gray-50 p-3 text-sm text-gray-600">{holidayLoading?'Loading holiday configuration…':recurringLabel.length?`${recurringLabel.join(', ')} ${recurringLabel.length===1?'is':'are'} recurring holiday${recurringLabel.length===1?'':'s'}. Declared holidays are also shown on their specific dates.`:'No recurring weekly holidays configured. Declared holidays are shown on their specific dates.'}</div>{message&&<div className="mb-4 rounded-lg bg-green-50 p-3 text-sm text-green-700">{message}</div>}<div className="grid grid-cols-1 gap-4 xl:grid-cols-3"><div className="min-w-0 rounded-lg bg-white p-3 shadow-sm sm:p-5 xl:col-span-2"><div className="mb-4 flex items-center justify-between"><button type="button" onClick={()=>changeMonth(-1)} className="rounded-md bg-gray-200 px-3 py-2">←</button><h4 className="text-base font-semibold sm:text-xl">{getMonthName(currentDate.getMonth())} {currentDate.getFullYear()}</h4><button type="button" onClick={()=>changeMonth(1)} className="rounded-md bg-gray-200 px-3 py-2">→</button></div><div className="grid grid-cols-7 text-center text-[10px] font-bold text-gray-500 sm:text-xs">{['Sun','Mon','Tue','Wed','Thu','Fri','Sat'].map(d=><div key={d}>{d}</div>)}</div><div className="mt-1 grid grid-cols-7 overflow-hidden rounded border">{days}</div>{selectedDate&&<div className="mt-4 rounded-lg border bg-gray-50 p-3"><p className="font-semibold">Selected date: {selectedDate}</p><p className="mt-1 text-sm text-gray-600">{selectedHoliday?'Holiday':'Working day'}</p>{!selectedRecurring&&<button type="button" disabled={saving||holidayLoading} onClick={()=>void toggleHoliday(selectedDate)} className="mt-3 min-h-11 w-full rounded-lg bg-primary-600 px-4 py-2.5 text-sm font-semibold text-white disabled:opacity-50 sm:w-auto">{saving?'Saving…':declaredDates.includes(selectedDate)?'Remove Holiday':'Declare Holiday'}</button>}</div>}</div><div className="min-w-0 rounded-lg bg-white p-3 shadow-sm sm:p-5"><h4 className="mb-4 text-lg font-bold text-gray-800">Details for {selectedDate||'selected date'}</h4>{selectedOrders.length?<div className="max-h-[32rem] space-y-3 overflow-y-auto">{selectedOrders.map(order=>{const normal=normalTotal(order);const guest=guestTotal(order);const hasGuest=Object.keys(order.guestItems||{}).some(c=>order.guestItems?.[c]);const identity=identityForOrder(order);const memberAdjustments=selectedAdjustments.filter(a=>a.employeeCode===identity);return <div key={order.id} className="rounded-lg border p-3"><p className="font-bold text-gray-800">{employeeName(order)}</p><p className="text-xs text-gray-500">{order.memberMobileSnapshot?`Mobile: ${order.memberMobileSnapshot} · `:''}Status: {order.status||'active'}</p>{Object.keys(order.items||{}).some(c=>order.items[c])&&<div className="mt-2 text-sm"><div className="font-semibold text-gray-700">MEMBER ORDER</div><ul className="mt-1 space-y-1 text-gray-600">{Object.keys(order.items||{}).filter(c=>order.items[c]).map(c=>{const q=Math.max(1,Number(order.itemQuantities?.[c]||1));const u=Number(order.itemPrices?.[c]||0);return <li key={c}>{order.itemNames?.[c]||c} × {q} · ₹{(u*q).toFixed(2)}</li>})}</ul><p className="mt-1 text-right font-semibold">Member Total: ₹{normal.toFixed(2)}</p></div>}{hasGuest&&<div className="mt-3 border-t pt-2 text-sm"><div className="font-semibold text-gray-700">GUEST ORDER</div><p className="text-xs text-gray-500">Guests: {order.guestCount||0}</p><ul className="mt-1 space-y-1 text-gray-600">{Object.keys(order.guestItems||{}).filter(c=>order.guestItems?.[c]).map(c=>{const q=Math.max(1,Number(order.guestItemQuantities?.[c]||1));const u=Number(order.guestItemPrices?.[c]||0);return <li key={c}>{order.guestItemNames?.[c]||order.itemNames?.[c]||c} × {q} · ₹{(u*q).toFixed(2)}</li>})}</ul><p className="mt-1 text-right font-semibold">Guest Total: ₹{guest.toFixed(2)}</p></div>}{memberAdjustments.length>0&&<div className="mt-3 border-t pt-2 text-sm"><div className="font-semibold text-primary-700">ADMIN ADDED AMOUNT</div>{memberAdjustments.map(a=><div key={a.id} className="mt-1 flex justify-between gap-3"><span>{a.description?.trim()||'Added Amount'}</span><b>₹{Number(a.amount).toFixed(2)}</b></div>)}</div>}</div>})}</div>:<p className="text-sm text-gray-500">{selectedDate?'No normal orders for this date.':'Select a date to see orders.'}</p>}{adjustmentOnly.length>0&&<div className="mt-4 border-t pt-3"><div className="mb-2 font-semibold text-gray-700">ADMIN ADDED AMOUNTS</div><div className="space-y-2">{adjustmentOnly.map(a=>{const member=users.find(u=>u.identityId===a.employeeCode||u.id===a.employeeCode);const name=historicalMemberName(a.member_name_snapshot,member?.name,member?.status);return <div key={a.id} className="rounded-lg border bg-gray-50 p-3"><div className="font-semibold text-gray-800">{name}</div><div className="flex justify-between gap-3 text-sm"><span>{a.description?.trim()||'Added Amount'}</span><b>₹{Number(a.amount).toFixed(2)}</b></div></div>})}</div></div>}</div></div></div>};export default AllOrdersCalendar;