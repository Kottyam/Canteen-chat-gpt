import React,{useState}from'react';
import{useData}from'../../context/DataContext';
import{useAuth}from'../../context/AuthContext';
import{User,Status}from'../../types';
import{saveEmployee,deleteEmployee,updateEmployee,resetEmployeePassword,loadMemberDefaultPassword}from'../../services/supabaseSync';
import{loadScopedUsers}from'../../services/supabaseData';
import{supabase,normalizeMobileNumber}from'../../supabase';

const invokeMemberFunction=async(body:Record<string,unknown>)=>{if(!supabase)throw new Error('Supabase is not enabled.');const{data,error}=await supabase.functions.invoke('create-employee',{body});if(error){try{const context=(error as any)?.context;if(context?.json){const payload=await context.json();if(payload?.error)throw new Error(String(payload.error));}}catch(parsed:any){if(parsed?.message&&!String(parsed.message).includes('body'))throw parsed;}throw error;}if(data?.error)throw new Error(String(data.error));return data as any};

const EmployeeManagement:React.FC=()=>{const{users,setUsers}=useData();const{user:authUser}=useAuth();const mobileMode=authUser?.memberLoginMode==='mobile';const[form,setForm]=useState({name:'',sr:'',mobile:''});const[error,setError]=useState('');const[success,setSuccess]=useState('');const[busy,setBusy]=useState<string|null>(null);const[defaultPassword,setDefaultPassword]=useState('123456');const[editingId,setEditingId]=useState<string|null>(null);const[editName,setEditName]=useState('');const[editMobile,setEditMobile]=useState('');const employees=users.filter(u=>u.role==='employee'&&u.status!=='deleted');
 const refreshUsers=async()=>{if(!authUser?.canteenId)throw new Error('Authenticated Canteen is not resolved.');const refreshed=await loadScopedUsers(authUser.canteenId);setUsers(refreshed)};
 const handleAdd=async(e:React.FormEvent)=>{e.preventDefault();setError('');setSuccess('');if(mobileMode){const mobile=normalizeMobileNumber(form.mobile);if(!form.name.trim()){setError('Member Name is required.');return}if(!/^[6-9][0-9]{9}$/.test(mobile)){setError('Please enter a valid Mobile Number.');return}try{await invokeMemberFunction({action:'create',full_name:form.name.trim(),mobile_number:mobile});await refreshUsers();setForm({name:'',sr:'',mobile:''});setSuccess(`Member Created Successfully\nName: ${form.name.trim()}\nMobile Number: ${mobile}`)}catch(err:any){setError(err?.message||'Unable to create Member account. Please try again.')}return}if(!form.name.trim()||!/^[0-9]{5}$/.test(form.sr)){setError('Enter Member name and a 5-digit SR Number.');return}if(employees.some(u=>u.id===form.sr)){setError('SR Number already exists.');return}const password=await loadMemberDefaultPassword().catch(()=>defaultPassword);const employee:User={id:form.sr,name:form.name.trim(),mobile:form.mobile.trim(),password,role:'employee',status:'active' as Status,isFirstLogin:true,memberLoginMode:'sr'};try{await saveEmployee(employee);setUsers(prev=>[...prev.filter(u=>u.id!==employee.id),employee]);setForm({name:'',sr:'',mobile:''});setSuccess(`Member ${employee.name} added. Default Password: ${password}`)}catch(err:any){setError(`Could not save Member: ${err?.message||'Supabase error'}`)}};
 const toggleStatus=async(id:string)=>{const emp=employees.find(u=>u.id===id);if(!emp)return;const next={...emp,status:emp.status==='active'?'blocked':'active'}as User;try{await updateEmployee(next);setUsers(prev=>prev.map(u=>u.id===id?next:u));setSuccess('Member status saved.')}catch(err:any){setError(err?.message||'Could not update Member.')}};
 const resetPassword=async(id:string)=>{const emp=employees.find(u=>u.id===id);if(!emp)return;if(!window.confirm(mobileMode?'Reset this Member to the current Mobile Number as the default password?':'Reset this Member to the default password? They will be required to set a new password on their next login.'))return;setBusy(id);setError('');setSuccess('');try{if(mobileMode){await invokeMemberFunction({action:'reset_password',mobile_number:normalizeMobileNumber(emp.mobile)});setSuccess('Password reset to the Member’s current Mobile Number.')}else{const password=await loadMemberDefaultPassword();await resetEmployeePassword(id,password);setUsers(prev=>prev.map(u=>u.id===id?{...u,password,isFirstLogin:true}:u));setSuccess(`Password reset to the current default password: ${password}`)}}catch(err:any){setError(err?.message||'Could not reset password.')}finally{setBusy(null)}};
 const startEdit=(emp:User)=>{setEditingId(emp.id);setEditName(emp.name);setEditMobile(emp.mobile||'');setError('');setSuccess('')};
 const saveEdit=async()=>{const emp=employees.find(u=>u.id===editingId);if(!emp)return;const mobile=normalizeMobileNumber(editMobile);if(!editName.trim()){setError('Member Name is required.');return}if(!/^[6-9][0-9]{9}$/.test(mobile)){setError('Please enter a valid Mobile Number.');return}setBusy(emp.id);setError('');setSuccess('');try{await invokeMemberFunction({action:'update_mobile',current_mobile:normalizeMobileNumber(emp.mobile),mobile_number:mobile,full_name:editName.trim()});await refreshUsers();setEditingId(null);setSuccess('Member details updated successfully.')}catch(err:any){setError(err?.message||'Could not save Member details.')}finally{setBusy(null)}};
 const remove=async(id:string)=>{const emp=employees.find(u=>u.id===id);if(!emp||!window.confirm(mobileMode?`Delete Member "${emp.name}" from the Member list? Their order history will be retained for the configured two-month retention period.`:`Delete Member "${emp.name}" (SR ${emp.id}) from the Member list? Their order history will be retained for the configured two-month retention period.`))return;setBusy(id);setError('');setSuccess('');try{await deleteEmployee(id);setUsers(prev=>prev.filter(u=>u.id!==id));setSuccess(`${emp.name} was deleted from the Member list. Their order history is preserved for the configured two-month retention period.`)}catch(err:any){setError(err?.message||'Could not delete Member.')}finally{setBusy(null)}};
 return <div className="w-full min-w-0"><h3 className="mb-5 text-xl font-bold text-gray-800 sm:text-2xl">Member Management</h3><form onSubmit={handleAdd} className="mb-6 w-full rounded-lg border bg-gray-50 p-3 sm:p-4"><h4 className="mb-3 font-semibold">{mobileMode?'Add New Member':'Add New Member'}</h4>{error&&<div className="mb-3 whitespace-pre-line rounded-md bg-red-100 p-3 text-sm text-red-700">{error}</div>}{success&&<div className="mb-3 whitespace-pre-line rounded-md bg-green-100 p-3 text-sm text-green-700">{success}</div>}{mobileMode&&<div className="mb-4 rounded-lg border border-primary-200 bg-primary-50 p-4 text-sm text-gray-700"><p className="font-bold text-primary-800">MEMBER LOGIN INFORMATION</p><div className="mt-2 space-y-1"><p><strong>User ID:</strong> Member's Mobile Number</p><p><strong>Default Password:</strong> Member's Mobile Number</p><p className="pt-1"><strong>Important:</strong> The Default Password will always be the Member's Mobile Number.</p><p>If the password is reset to default, it will be reset to the Member's current Mobile Number.</p><p>Mobile Number must be unique across GoCanteen.</p><p>If this Mobile Number is already registered, the Member cannot be created.</p></div></div>}{/* existing form markup */}<div className="grid grid-cols-1 gap-3 md:grid-cols-3"><input required placeholder="Member Name" value={form.name} onChange={e=>setForm({...form,name:e.target.value})} className="w-full rounded-md border px-3 py-2.5"/>{!mobileMode&&<input required inputMode="numeric" maxLength={5} placeholder="5-digit SR Number" value={form.sr} onChange={e=>setForm({...form,sr:e.target.value.replace(/\D/g,'').slice(0,5)})} className="w-full rounded-md border px-3 py-2.5"/>}<input required={mobileMode} inputMode="tel" placeholder="Mobile Number" value={form.mobile} onChange={e=>setForm({...form,mobile:e.target.value})} className="w-full rounded-md border px-3 py-2.5"/></div><button type="submit" className="mt-3 w-full rounded-md bg-primary-600 px-4 py-2.5 text-sm font-medium text-white sm:w-auto">{mobileMode?'Create Member':'Add Member'}</button></form><div className="space-y-3">{employees.map(emp=><div key={emp.id} className="rounded-lg border bg-white p-4 shadow-sm"><p className="font-semibold">{emp.name}</p><p className="text-sm text-gray-500">{mobileMode?`Mobile Number: ${emp.mobile||'—'}`:`SR: ${emp.id} · Mobile: ${emp.mobile||'—'}`}</p><p className="text-sm capitalize">Status: {emp.status}</p>{editingId===emp.id&&mobileMode&&<div className="mt-3 space-y-2 rounded-md bg-gray-50 p-3"><input value={editName} onChange={e=>setEditName(e.target.value)} placeholder="Member Name" className="w-full rounded-md border px-3 py-2.5"/><input value={editMobile} onChange={e=>setEditMobile(e.target.value)} inputMode="tel" placeholder="Mobile Number" className="w-full rounded-md border px-3 py-2.5"/><div className="grid grid-cols-2 gap-2"><button type="button" disabled={busy===emp.id} onClick={()=>void saveEdit()} className="rounded-md bg-primary-600 px-3 py-2 text-sm text-white">{busy===emp.id?'Saving…':'Save'}</button><button type="button" disabled={busy===emp.id} onClick={()=>setEditingId(null)} className="rounded-md border px-3 py-2 text-sm">Cancel</button></div></div>}<div className="mt-3 grid grid-cols-1 gap-2 sm:grid-cols-3">{mobileMode&&<button onClick={()=>startEdit(emp)} disabled={busy===emp.id} className="rounded-md border px-3 py-2 text-sm text-gray-700">Edit Member</button>}<button onClick={()=>toggleStatus(emp.id)} className="rounded-md bg-yellow-500 px-3 py-2 text-sm text-white">{emp.status==='active'?'Block':'Unblock'}</button><button disabled={busy===emp.id} onClick={()=>void resetPassword(emp.id)} className="rounded-md bg-primary-500 px-3 py-2 text-sm text-white disabled:opacity-50">{busy===emp.id?'Resetting…':'Reset Password'}</button><button disabled={busy===emp.id} onClick={()=>void remove(emp.id)} className="rounded-md bg-red-600 px-3 py-2 text-sm text-white disabled:opacity-50">{busy===emp.id?'Deleting…':'Delete Member'}</button></div></div>)}</div></div>};export default EmployeeManagement;
