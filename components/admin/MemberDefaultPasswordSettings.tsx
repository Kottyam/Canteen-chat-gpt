import React,{useEffect,useState}from'react';import{loadMemberDefaultPassword,saveMemberDefaultPassword}from'../../services/supabaseSync';
const MemberDefaultPasswordSettings:React.FC=()=>{const[value,setValue]=useState('');const[busy,setBusy]=useState(true);const[saving,setSaving]=useState(false);const[message,setMessage]=useState('');const[error,setError]=useState('');useEffect(()=>{void loadMemberDefaultPassword().then(p=>setValue(p)).catch(e=>setError(e?.message||'Could not load the default password.')).finally(()=>setBusy(false))},[]);const save=async()=>{setError('');setMessage('');if(value.length<6){setError('Password must contain at least 6 characters.');return}setSaving(true);try{await saveMemberDefaultPassword(value);setMessage('Member Default Password saved.')}catch(e:any){setError(e?.message||'Could not save the default password.')}finally{setSaving(false)}};return <div className="rounded-lg border bg-gray-50 p-4"><h4 className="font-bold">Member Default Password</h4><p className="mt-1 text-sm text-gray-500">Used automatically when a new Member is created or a Member password is reset.</p>{error&&<div className="mt-3 rounded-md bg-red-100 p-3 text-sm text-red-700">{error}</div>}{message&&<div className="mt-3 rounded-md bg-green-100 p-3 text-sm text-green-700">{message}</div>}<div className="mt-3 flex flex-col gap-2 sm:flex-row"><input disabled={busy||saving} type="text" value={value} onChange={e=>setValue(e.target.value)} className="min-h-11 flex-1 rounded-md border px-3 py-2.5" placeholder="123456" autoComplete="off"/><button disabled={busy||saving} onClick={()=>void save()} className="min-h-11 rounded-md bg-primary-600 px-4 py-2.5 text-sm font-semibold text-white disabled:opacity-50">{saving?'Saving…':'Save'}</button></div></div>};export default MemberDefaultPasswordSettings;
