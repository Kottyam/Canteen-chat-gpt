import React,{useEffect,useMemo,useState}from'react';
import{useData}from'../../context/DataContext';
import{downloadMonthlyPdf,orderTotal,guestOrderTotal,uniqueOrderedDays}from'../../utils/pdf';
import{EmployeeAdjustmentForReport,loadEmployeeAdjustmentsForUsers}from'../../services/employeeAdjustments';
import{loadAdminBills,loadAdminBillPublishStates,publishEmployeeBill,MonthlyBill,BillPublishState}from'../../services/monthlyBills';

const pad=(n:number)=>String(n).padStart(2,'0');
const dateString=(d:Date)=>`${d.getFullYear()}-${pad(d.getMonth()+1)}-${pad(d.getDate())}`;
const displayDate=(value:string)=>new Date(`${value}T00:00:00`).toLocaleDateString('en-IN',{day:'numeric',month:'long',year:'numeric'});

const MonthlyReport:React.FC=()=>{
 const{users,orders,prices}=useData();
 const[period,setPeriod]=useState<'last'|'current'>('last');
 const[adjustments,setAdjustments]=useState<EmployeeAdjustmentForReport[]>([]);
 const[bills,setBills]=useState<MonthlyBill[]>([]);
 const[publishStates,setPublishStates]=useState<Record<string,BillPublishState>>({});
 const[loading,setLoading]=useState(false);const[publishing,setPublishing]=useState<string|null>(null);
 const now=new Date();
 const periodDates=useMemo(()=>{const currentEnd=dateString(now);if(period==='current'){const start=new Date(now.getFullYear(),now.getMonth(),1);return{start:dateString(start),end:currentEnd,month:now.getMonth()+1,year:now.getFullYear(),label:`${displayDate(dateString(start))} – ${displayDate(currentEnd)}`};}const start=new Date(now.getFullYear(),now.getMonth()-1,1);const end=new Date(now.getFullYear(),now.getMonth(),0);return{start:dateString(start),end:dateString(end),month:start.getMonth()+1,year:start.getFullYear(),label:`${displayDate(dateString(start))} – ${displayDate(dateString(end))}`};},[period]);
 const{start,end,month,year,label}=periodDates;
 const employees=useMemo(()=>users.filter(u=>u.role==='employee'),[users]);
 const reloadPublishingData=async()=>{try{const[loadedBills,loadedStates]=await Promise.all([loadAdminBills(month,year),loadAdminBillPublishStates(month,year)]);setBills(loadedBills);setPublishStates(loadedStates);}catch{setBills([]);setPublishStates({});}};
 useEffect(()=>{let alive=true;const load=async()=>{setLoading(true);try{const rows=await loadEmployeeAdjustmentsForUsers(employees.map(u=>u.id),start,end);if(alive)setAdjustments(rows);const[loadedBills,loadedStates]=await Promise.all([loadAdminBills(month,year),loadAdminBillPublishStates(month,year)]);if(alive){setBills(loadedBills);setPublishStates(loadedStates);}}catch{if(alive){setAdjustments([]);setBills([]);setPublishStates({});}}finally{if(alive)setLoading(false);}};void load();const timer=window.setInterval(load,10000);return()=>{alive=false;window.clearInterval(timer)};},[employees,start,end,month,year]);
 const data=useMemo(()=>employees.map(user=>{const orderKey=user.memberLoginMode==='mobile'?user.mobile:user.id;const mine=orders.filter(o=>o.employeeId===orderKey&&o.date>=start&&o.date<=end&&o.status!=='cancelled');const employeeFoodTotal=mine.reduce((sum,o)=>sum+orderTotal(o,prices),0);const guestFoodTotal=mine.reduce((sum,o)=>sum+guestOrderTotal(o,prices),0);const foodTotal=employeeFoodTotal+guestFoodTotal;const adminTotal=adjustments.filter(a=>a.employeeCode===user.id).reduce((sum,a)=>sum+Number(a.amount||0),0);const daysOrdered=uniqueOrderedDays(orders,orderKey,start,end);return{user,count:mine.filter(o=>Object.keys(o.items||{}).some(c=>o.items[c])).length,guestCount:mine.reduce((n,o)=>n+Object.values(o.guestItemQuantities||{}).reduce((s,q)=>s+Number(q||0),0),0),daysOrdered,employeeFoodTotal,guestFoodTotal,foodTotal,adminTotal,total:foodTotal+adminTotal};}).filter(row=>row.user.status!=='deleted'||row.count>0||row.guestCount>0||row.adminTotal>0),[employees,orders,prices,start,end,adjustments]);
 const publishedCount=bills.filter(b=>b.published).length;
 const billFor=(id:string)=>bills.find(b=>b.employee_code===id);
 const stateFor=(id:string)=>publishStates[id];
 const eligibleCount=data.filter(row=>stateFor(row.user.id)?.can_publish).length;
 const publishOne=async(row:any)=>{const state=stateFor(row.user.id);if(!state?.can_publish)return;setPublishing(row.user.id);try{await publishEmployeeBill(row.user.id,month,year,row.foodTotal,row.adminTotal,row.guestFoodTotal,row.daysOrdered,start,end);await reloadPublishingData();}catch(e:any){alert(e?.message||'Could not publish bill.');await reloadPublishingData();}finally{setPublishing(null)}};
 const publishAll=async()=>{setPublishing('all');const eligible=data.filter(row=>stateFor(row.user.id)?.can_publish);const results=await Promise.all(eligible.map(async(row)=>{try{await publishEmployeeBill(row.user.id,month,year,row.foodTotal,row.adminTotal,row.guestFoodTotal,row.daysOrdered,start,end);return{employee:row.user.name,ok:true};}catch(e:any){return{employee:row.user.name,ok:false,error:String(e?.message||'Could not publish bill.')}}));await reloadPublishingData();const succeeded=results.filter(r=>r.ok).length;const failed=results.filter(r=>!r.ok);if(failed.length){alert(`${succeeded} published. ${failed.length} failed:\n${failed.map(r=>`${r.employee}: ${r.error}`).join('\n')}`);}else{alert(succeeded?`${succeeded} eligible bill${succeeded===1?' was':'s were'} published.`:'No new amounts were available to publish.');}setPublishing(null)};
 const grandTotal=data.reduce((sum,row)=>sum+(stateFor(row.user.id)?.current_total??row.total),0);
 return <div className="w-full min-w-0"><div className="mb-5"><h3 className="text-xl font-bold text-gray-800 sm:text-2xl">Monthly Reports &amp; Bills</h3><p className="mt-1 text-sm text-gray-500">Select the billing period before preparing or publishing bills.</p></div><div className="mb-5 rounded-lg bg-gray-50 p-3 sm:p-4"><div className="mb-3 grid grid-cols-2 gap-2"><button type="button" onClick={()=>setPeriod('last')} className={`min-h-11 rounded-lg px-3 py-2.5 text-sm font-semibold ${period==='last'?'bg-white text-primary-700 shadow-sm':'bg-gray-100 text-gray-600'}`}>Last Month</button><button type="button" onClick={()=>setPeriod('current')} className={`min-h-11 rounded-lg px-3 py-2.5 text-sm font-semibold ${period==='current'?'bg-white text-primary-700 shadow-sm':'bg-gray-100 text-gray-600'}`}>Current Month</button></div><div className="text-sm font-semibold text-gray-700">Billing Period: {label}</div><div className="mt-2 flex flex-col gap-2 sm:flex-row sm:flex-wrap"><button type="button" disabled={loading} onClick={()=>void downloadMonthlyPdf(month-1,year,users,orders,prices,adjustments,start,end)} className="w-full rounded-md bg-primary-600 px-4 py-2.5 text-sm font-medium text-white disabled:opacity-50 sm:w-auto">Generate Monthly PDF</button><button type="button" disabled={publishing!==null||eligibleCount===0} onClick={()=>void publishAll()} className="w-full rounded-md bg-primary-700 px-4 py-2.5 text-sm font-semibold text-white disabled:opacity-50 sm:w-auto">{publishing==='all'?'Publishing…':eligibleCount?'Publish Eligible Bills':'No New Amounts to Publish'}</button></div></div><div className="mb-4 grid grid-cols-2 gap-3 sm:grid-cols-3"><div className="rounded-lg border bg-white p-4"><div className="text-sm text-gray-500">Employees</div><div className="text-xl font-bold">{data.length}</div></div><div className="rounded-lg border bg-white p-4"><div className="text-sm text-gray-500">Published</div><div className="text-xl font-bold">{publishedCount} / {data.length}</div></div><div className="col-span-2 rounded-lg border bg-white p-4 sm:col-span-1"><div className="text-sm text-gray-500">Period Total</div><div className="text-xl font-bold text-primary-700">₹{grandTotal.toFixed(2)}</div></div></div><div className="space-y-3">{data.map(row=>{const existing=billFor(row.user.id);const state=stateFor(row.user.id);const currentTotal=state?.current_total??row.total;const newAmount=state?.new_amount??(existing?.published?0:row.total);const canPublish=Boolean(state?.can_publish);const message=state?.publish_message||(!canPublish&&existing?.published?'No new amount since the last publication.':null);return <div key={row.user.id} className="rounded-xl border bg-white p-4"><div className="flex flex-wrap items-start justify-between gap-3"><div><div className="font-semibold text-gray-800">{row.user.name}</div><div className="text-sm text-gray-500">{row.user.memberLoginMode==='mobile'?`Mobile Number: ${row.user.mobile||'—'}`:`SR: ${row.user.id} · Mobile: ${row.user.mobile||'—'}`} · Employee Orders: {row.count} · Guest Meals: {row.guestCount}</div></div><div className="text-right"><div className="font-bold">₹{currentTotal.toFixed(2)}</div><div className={`text-xs font-semibold ${existing?.published?'text-primary-700':'text-gray-500'}`}>{existing?.published?'Published':'Not Published'}</div></div></div><div className="mt-3 rounded bg-gray-50 p-2 text-sm"><span className="text-gray-500">Billing Period</span><br/><b>{label}</b></div><div className="mt-2 grid grid-cols-2 gap-2 text-sm sm:grid-cols-4"><div className="rounded bg-gray-50 p-2">Number of Days Ordered<br/><b>{row.daysOrdered}</b></div><div className="rounded bg-gray-50 p-2">Employee Food<br/><b>₹{row.employeeFoodTotal.toFixed(2)}</b></div><div className="rounded bg-gray-50 p-2">Guest Food<br/><b>₹{row.guestFoodTotal.toFixed(2)}</b></div><div className="rounded bg-gray-50 p-2">Admin Added<br/><b>₹{row.adminTotal.toFixed(2)}</b></div></div>{canPublish&&existing?.published&&<div className="mt-3 rounded-lg border border-primary-100 bg-primary-50 p-3 text-sm font-semibold text-primary-800">New amount available: ₹{newAmount.toFixed(2)}</div>}{!canPublish&&existing?.published&&<div className="mt-3 rounded-lg bg-gray-50 p-3 text-sm font-semibold text-gray-600">{message||'No new amount since the last publication.'}</div>}<div className="mt-3 flex justify-end"><button type="button" onClick={()=>void publishOne(row)} disabled={publishing!==null||!canPublish} className="rounded-lg bg-primary-600 px-4 py-2 text-sm font-semibold text-white disabled:opacity-50">{publishing===row.user.id?'Publishing…':existing?.published?(canPublish?`Republish ₹${newAmount.toFixed(2)}`:'Already Published'):`Publish ₹${newAmount.toFixed(2)}`}</button></div></div>})}</div></div>;
};
export default MonthlyReport;
