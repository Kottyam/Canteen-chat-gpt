import React,{useEffect,useMemo,useState}from'react';import{useData}from'../../context/DataContext';import{downloadMonthlyPdf,orderTotal,guestOrderTotal,uniqueOrderedDays}from'../../utils/pdf';import{EmployeeAdjustmentForReport,loadEmployeeAdjustmentsForUsers}from'../../services/employeeAdjustments';import{loadAdminBills,loadAdminBillPublishStates,publishEmployeeBill,MonthlyBill,BillPublishState}from'../../services/monthlyBills';import{loadMonthlyBundleSummary,MonthlyBundleSummary}from'../../services/bundles';import{historicalMemberName,memberDisplayMobile}from'../../utils/memberIdentity';
const pad=(n:number)=>String(n).padStart(2,'0');const dateString=(d:Date)=>`${d.getFullYear()}-${pad(d.getMonth()+1)}-${pad(d.getDate())}`;const displayDate=(value:string)=>new Date(`${value}T00:00:00`).toLocaleDateString('en-IN',{day:'numeric',month:'long',year:'numeric'});
const MonthlyReport:React.FC=()=>{const{users,orders,prices}=useData();const[period,setPeriod]=useState<'last'|'current'>('last');const[adjustments,setAdjustments]=useState<EmployeeAdjustmentForReport[]>([]);const[bills,setBills]=useState<MonthlyBill[]>([]);const[publishStates,setPublishStates]=useState<Record<string,BillPublishState>>({});const[bundleSummary,setBundleSummary]=useState<Record<string,MonthlyBundleSummary>>({});const[loading,setLoading]=useState(false);const[publishing,setPublishing]=useState<string|null>(null);const now=new Date();const periodDates=useMemo(()=>{const currentEnd=dateString(now);if(period==='current'){const start=new Date(now.getFullYear(),now.getMonth(),1);return{start:dateString(start),end:currentEnd,month:now.getMonth()+1,year:now.getFullYear(),label:`${displayDate(dateString(start))} – ${displayDate(currentEnd)}`}}const start=new Date(now.getFullYear(),now.getMonth()-1,1);const end=new Date(now.getFullYear(),now.getMonth(),0);return{start:dateString(start),end:dateString(end),month:start.getMonth()+1,year:start.getFullYear(),label:`${displayDate(dateString(start))} – ${displayDate(dateString(end))}`}},[period]);const{start,end,month,year,label}=periodDates;const employees=useMemo(()=>users.filter(u=>u.role==='employee'),[users]);const loadAll=async()=>{const[loadedBills,loadedStates,loadedBundles]=await Promise.all([loadAdminBills(month,year),loadAdminBillPublishStates(month,year),loadMonthlyBundleSummary(month,year)]);setBills(loadedBills);setPublishStates(loadedStates);setBundleSummary(Object.fromEntries(loadedBundles.map(x=>[x.member_id,x])))};useEffect(()=>{let alive=true;const load=async()=>{setLoading(true);try{const rows=await loadEmployeeAdjustmentsForUsers(employees.map(u=>u.id),start,end);if(alive)setAdjustments(rows);await loadAll()}catch{if(alive){setAdjustments([]);setBills([]);setPublishStates({});setBundleSummary({})}}finally{if(alive)setLoading(false)}};void load();const timer=window.setInterval(load,10000);return()=>{alive=false;window.clearInterval(timer)}},[employees,start,end,month,year]);const data=useMemo(()=>employees.map(user=>{const orderKey=user.memberLoginMode==='mobile'?user.mobile:user.id;const mine=orders.filter(o=>o.employeeId===orderKey&&o.date>=start&&o.date<=end&&o.status!=='cancelled');const employeeFoodTotal=mine.reduce((sum,o)=>sum+orderTotal(o,prices),0);const guestFoodTotal=mine.reduce((sum,o)=>sum+guestOrderTotal(o,prices),0);const foodTotal=employeeFoodTotal+guestFoodTotal;const adminTotal=adjustments.filter(a=>a.employeeCode===user.id).reduce((sum,a)=>sum+Number(a.amount||0),0);const daysOrdered=uniqueOrderedDays(orders,orderKey,start,end);const guestCount=mine.reduce((n,o)=>n+Object.values(o.guestItemQuantities||{}).reduce((s,q)=>s+Number(q||0),0),0);const bundle=bundleSummary[user.id]?.bundle_total||0;return{user,count:mine.filter(o=>Object.keys(o.items||{}).some(c=>o.items[c])).length,guestCount,daysOrdered,employeeFoodTotal,guestFoodTotal,foodTotal,adminTotal,bundleTotal:bundle,total:foodTotal+adminTotal+bundle}}).filter(row=>row.user.status!=='deleted'||row.count>0||row.guestCount>0||row.adminTotal>0||row.bundleTotal>0||bills.some(b=>b.employee_id&&b.employee_code===row.user.id)),[employees,orders,prices,start,end,adjustments,bills,bundleSummary]);const publishedCount=bills.filter(b=>b.published).length;const billFor=(id:string)=>bills.find(b=>b.employee_code===id);const stateFor=(id:string)=>publishStates[id];const eligibleCount=data.filter(row=>row.user.status==='active'&&stateFor(row.user.id)?.can_publish).length;const publishOne=async(row:any)=>{if(row.user.status!=='active')return;const state=stateFor(row.user.id);if(!state?.can_publish)return;setPublishing(row.user.id);try{await publishEmployeeBill(row.user.id,month,year,row.foodTotal,row.adminTotal,row.guestFoodTotal,row.daysOrdered,start,end);await loadAll()}catch(e:any){alert(e?.message||'Could not publish bill.');await loadAll()}finally{setPublishing(null)}};const publishAll=async()=>{setPublishing('all');const eligible=data.filter(row=>row.user.status==='active'&&stateFor(row.user.id)?.can_publish);const results:{employee:string;ok:boolean;error?:string}[]=[];for(const row of eligible){try{await publishEmployeeBill(row.user.id,month,year,row.foodTotal,row.adminTotal,row.guestFoodTotal,row.daysOrdered,start,end);results.push({employee:row.user.name,ok:true})}catch(e:any){results.push({employee:row.user.name,ok:false,error:String(e?.message||'Could not publish bill.')})}}await loadAll();const failed=results.filter(r=>!r.ok);if(failed.length)alert(`${results.length-failed.length} published. ${failed.length} failed:\n${failed.map(r=>`${r.employee}: ${r.error}`).join('\n')}`);else alert(results.length?`${results.length} eligible bill${results.length===1?' was':'s were'} published.`:'No new amounts were available to publish.');setPublishing(null)};const grandTotal=data.reduce((sum,row)=>sum+(stateFor(row.user.id)?.current_total??row.total),0);const pdfBundles=Object.values(bundleSummary);
return <div className="w-full min-w-0"><div className="mb-5"><h3 className="text-xl font-bold text-gray-800 sm:text-2xl">Monthly Reports &amp; Bills</h3><p className="mt-1 text-sm text-gray-500">Normal food, Admin Added and Bundles remain separately identifiable.</p></div><div className="mb-5 rounded-lg bg-gray-50 p-3 sm:p-4"><div className="mb-3 grid grid-cols-2 gap-2"><button type="button" onClick={()=>setPeriod('last')} className={`min-h-11 rounded-lg px-3 py-2.5 text-sm font-semibold ${period==='last'?'bg-white text-primary-700 shadow-sm':'bg-gray-100 text-gray-600'}`}>Last Month</button><button type="button" onClick={()=>setPeriod('current')} className={`min-h-11 rounded-lg px-3 py-2.5 text-sm font-semibold ${period==='current'?'bg-white text-primary-700 shadow-sm':'bg-gray-100 text-gray-600'}`}>Current Month</button></div><div className="text-sm font-semibold text-gray-700">Billing Period: {label}</div><div className="mt-2 flex flex-col gap-2 sm:flex-row sm:flex-wrap"><button type="button" disabled={loading} onClick={()=>void downloadMonthlyPdf(month,year,users,orders,prices,adjustments,start,end,pdfBundles)} className="w-full rounded-md bg-primary-600 px-4 py-2.5 text-sm font-medium text-white disabled:opacity-50 sm:w-auto">Generate Monthly PDF</button><button type="button" disabled={publishing!==null||eligibleCount===0} onClick={()=>void publishAll()} className="w-full rounded-md bg-primary-700 px-4 py-2.5 text-sm font-semibold text-white disabled:opacity-50 sm:w-auto">{publishing==='all'?'Publishing…':eligibleCount?'Publish Eligible Bills':'No New Amounts to Publish'}</button></div></div><div className="mb-4 grid grid-cols-2 gap-3 sm:grid-cols-4"><div className="rounded-lg border bg-white p-4"><div className="text-sm text-gray-500">Members</div><div className="text-xl font-bold">{data.length}</div></div><div className="rounded-lg border bg-white p-4"><div className="text-sm text-gray-500">Published</div><div className="text-xl font-bold">{publishedCount} / {data.length}</div></div><div className="rounded-lg border bg-white p-4"><div className="text-sm text-gray-500">Bundle Revenue</div><div className="text-xl font-bold">₹{data.reduce((s,r)=>s+r.bundleTotal,0).toFixed(2)}</div></div><div className="rounded-lg border bg-white p-4"><div className="text-sm text-gray-500">Period Total</div><div className="text-xl font-bold text-primary-700">₹{grandTotal.toFixed(2)}</div></div></div><div className="space-y-3">{data.map(row=>{const existing=billFor(row.user.id);const state=stateFor(row.user.id);const currentTotal=state?.current_total??row.total;const newAmount=state?.new_amount??(existing?.published?0:row.total);const canPublish=row.user.status==='active'&&Boolean(state?.can_publish);const message=state?.publish_message||(!canPublish&&existing?.published?'No new amount since the last publication.':row.user.status==='deleted'?'Deleted Member — historical record only; no new publication is allowed.':null);const name=historicalMemberName(null,row.user.name,row.user.status);return <div key={row.user.id} className="rounded-xl border bg-white p-4"><div className="flex flex-wrap items-start justify-between gap-3"><div><div className="font-semibold text-gray-800">{name}</div><div className="text-sm text-gray-500">{row.user.memberLoginMode==='mobile'?`Mobile Number: ${memberDisplayMobile({mobile_number:row.user.mobile})||'—'}`:`SR: ${row.user.id} · Mobile: ${row.user.mobile||'—'}`} · Member Orders: {row.count} · Guest Meals: {row.guestCount}</div></div><div className="text-right"><div className="font-bold">₹{currentTotal.toFixed(2)}</div><div className={`text-xs font-semibold ${existing?.published?'text-primary-700':'text-gray-500'}`}>{existing?.published?'Published':'Not Published'}</div></div></div><div className="mt-3 grid grid-cols-2 gap-2 text-sm sm:grid-cols-4"><div className="rounded bg-gray-50 p-2">Number of Days Ordered<br/><b>{row.daysOrdered}</b></div><div className="rounded bg-gray-50 p-2">Member Food<br/><b>₹{row.employeeFoodTotal.toFixed(2)}</b></div><div className="rounded bg-gray-50 p-2">Admin Added<br/><b>₹{row.adminTotal.toFixed(2)}</b></div><div className="rounded bg-primary-50 p-2">Monthly Bundle<br/><b>₹{row.bundleTotal.toFixed(2)}</b></div></div>{row.bundleTotal>0&&<div className="mt-3 rounded-lg border border-primary-100 bg-primary-50 p-3 text-sm"><div className="font-semibold text-primary-800">ADMIN ADDED BUNDLED</div>{(bundleSummary[row.user.id]?.bundles||[]).map((b,i)=><div key={i} className="mt-1 flex justify-between gap-3"><span>{b.name}</span><span>₹{Number(b.amount).toFixed(2)}</span></div>)}</div>}{canPublish&&existing?.published&&<div className="mt-3 rounded-lg border border-primary-100 bg-primary-50 p-3 text-sm font-semibold text-primary-800">New amount available: ₹{newAmount.toFixed(2)}</div>}{!canPublish&&<div className="mt-3 rounded-lg bg-gray-50 p-3 text-sm font-semibold text-gray-600">{message||'Not eligible for new publication.'}</div>}<div className="mt-3 flex justify-between gap-3"><div className="text-sm text-gray-500">Total: ₹{currentTotal.toFixed(2)}</div><button type="button" onClick={()=>void publishOne(row)} disabled={publishing!==null||!canPublish} className="rounded-lg bg-primary-600 px-4 py-2 text-sm font-semibold text-white disabled:opacity-50">{publishing===row.user.id?'Publishing…':existing?.published?(canPublish?`Republish ₹${newAmount.toFixed(2)}`:'Not Publishable'):`Publish ₹${newAmount.toFixed(2)}`}</button></div></div>})}</div></div>};export default MonthlyReport;
