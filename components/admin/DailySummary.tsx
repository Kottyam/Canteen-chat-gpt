import React,{useEffect,useMemo,useState}from'react';import {useData}from'../../context/DataContext';import {downloadDailyPdf,orderTotal,guestOrderTotal,orderText}from'../../utils/pdf';import {formatDate}from'../../utils/helpers';import {EmployeeAdjustmentForReport,loadEmployeeAdjustmentsForUsers}from'../../services/employeeAdjustments';
const DailySummary:React.FC=()=>{const{orders,users,prices}=useData();const[selectedDate,setSelectedDate]=useState(formatDate(new Date()));const[adjustments,setAdjustments]=useState<EmployeeAdjustmentForReport[]>([]);const[loading,setLoading]=useState(false);useEffect(()=>{let alive=true;const load=async()=>{setLoading(true);try{const rows=await loadEmployeeAdjustmentsForUsers(users.filter(u=>u.role==='employee').map(u=>u.id),selectedDate,selectedDate);if(alive)setAdjustments(rows)}catch{if(alive)setAdjustments([])}finally{if(alive)setLoading(false)}};void load();return()=>{alive=false}},[users,selectedDate]);const day=useMemo(()=>orders.filter(o=>o.date===selectedDate&&o.status!=='cancelled'),[orders,selectedDate]);const extras=useMemo(()=>adjustments.filter(a=>a.adjustment_date===selectedDate),[adjustments,selectedDate]);const extraByEmployee=useMemo(()=>{const m:Record<string,number>={};extras.forEach(a=>m[a.employeeCode]=(m[a.employeeCode]||0)+Number(a.amount||0));return m},[extras]);const dynamicItems=useMemo(()=>{const m=new Map<string,{name:string,count:number}>();day.forEach(o=>Object.keys(o.items).forEach(code=>{if(!o.items[code])return;const name=o.itemNames?.[code]||code;const x=m.get(code)||{name,count:0};x.count++;m.set(code,x)}));return[...m.values()]},[day]);const total=day.reduce((sum,o)=>sum+orderTotal(o,prices)+guestOrderTotal(o,prices),0)+extras.reduce((sum,a)=>sum+Number(a.amount||0),0);const employeeCodes=[...new Set([...day.map(o=>o.employeeId),...extras.map(a=>a.employeeCode)])];return <div className="w-full min-w-0"><div className="mb-5"><h3 className="text-xl font-bold text-gray-800 sm:text-2xl">Daily Report</h3><p className="mt-1 text-sm text-gray-500">Select a date and generate the daily PDF report.</p></div><div className="mb-5 flex flex-col gap-2 rounded-lg bg-gray-50 p-3 sm:flex-row sm:items-center sm:p-4"><input type="date" value={selectedDate} onChange={e=>setSelectedDate(e.target.value)} className="w-full rounded-md border px-3 py-2.5 sm:w-auto"/><button type="button" disabled={loading} onClick={()=>void downloadDailyPdf(selectedDate,users,orders,prices,adjustments)} className="w-full rounded-md bg-primary-600 px-4 py-2.5 text-sm font-medium text-white disabled:opacity-50 sm:w-auto">{loading?'Loading…':'Generate Daily PDF'}</button></div><div className="mb-5 grid grid-cols-2 gap-3 sm:grid-cols-3 lg:grid-cols-5">{dynamicItems.map(item=><div key={item.name} className="rounded-lg border bg-white p-3"><div className="text-xs text-gray-500">{item.name}</div><div className="mt-1 text-2xl font-bold text-gray-800">{item.count}</div></div>)}</div><div className="mb-4 rounded-lg border bg-white p-4"><div className="flex flex-col gap-1 sm:flex-row sm:justify-between"><span className="font-semibold">Employees / Entries: {employeeCodes.length}</span><span className="font-bold text-primary-700">Total Collection: ₹{total.toFixed(2)}</span></div></div><div className="space-y-3 md:hidden">{employeeCodes.length===0?<div className="rounded-lg border p-4 text-sm text-gray-500">No orders or admin additions for this date.</div>:employeeCodes.map(code=>{const employeeOrders=day.filter(o=>o.employeeId===code);const employeeFood=employeeOrders.reduce((s,o)=>s+orderTotal(o,prices),0);const guestFood=employeeOrders.reduce((s,o)=>s+guestOrderTotal(o,prices),0);const extra=extraByEmployee[code]||0;return <div key={code} className="rounded-lg border bg-white p-4"><div className="font-semibold text-gray-800">{users.find(u=>u.id===code)?.name||code}</div><div className="text-sm text-gray-500">SR: {code}</div><div className="mt-2 text-sm">Employee Food: ₹{employeeFood.toFixed(2)}</div><div className="mt-1 text-sm">Guest Food: ₹{guestFood.toFixed(2)}</div><div className="mt-2 text-xs text-gray-500">{employeeOrders.flatMap(o=>[orderText(o),Object.keys(o.guestItems||{}).length?`Guest items: ${Object.keys(o.guestItems||{}).join(', ')}`:'']).filter(Boolean).join(' · ')}</div>{extra>0&&<div className="mt-2 text-sm font-medium text-primary-700">Admin Added: ₹{extra.toFixed(2)}</div>}<div className="mt-2 border-t pt-2 font-semibold">Employee Total: ₹{(employeeFood+guestFood+extra).toFixed(2)}</div></div>})}</div><div className="hidden overflow-x-auto rounded-lg border md:block"><table className="min-w-full bg-white"><thead className="bg-gray-50"><tr><th className="p-3 text-left text-sm">Name</th><th className="p-3 text-left text-sm">SR Number</th><th className="p-3 text-right text-sm">Employee Food</th><th className="p-3 text-right text-sm">Guest Food</th><th className="p-3 text-right text-sm">Admin Added</th><th className="p-3 text-right text-sm">Total</th></tr></thead><tbody>{employeeCodes.map(code=>{const employeeOrders=day.filter(o=>o.employeeId===code);const employeeFood=employeeOrders.reduce((s,o)=>s+orderTotal(o,prices),0);const guestFood=employeeOrders.reduce((s,o)=>s+guestOrderTotal(o,prices),0);const extra=extraByEmployee[code]||0;return <tr key={code} className="border-t"><td className="p-3">{users.find(u=>u.id===code)?.name||code}</td><td className="p-3">{code}</td><td className="p-3 text-right">₹{employeeFood.toFixed(2)}</td><td className="p-3 text-right">₹{guestFood.toFixed(2)}</td><td className="p-3 text-right">₹{extra.toFixed(2)}</td><td className="p-3 text-right font-semibold">₹{(employeeFood+guestFood+extra).toFixed(2)}</td></tr>})}</tbody></table></div></div>};export default DailySummary;
