import React,{useState}from'react';
import{supabase}from'../../supabase';
import{useAuth}from'../../context/AuthContext';

const CanteenOnboarding:React.FC=()=>{const{user,logout}=useAuth();const[name,setName]=useState(user?.canteenName||'');const[busy,setBusy]=useState(false);const[error,setError]=useState('');
 const submit=async(e:React.FormEvent)=>{e.preventDefault();if(!supabase)return;setError('');setBusy(true);try{const{error:rpcError}=await supabase.rpc('complete_google_admin_onboarding',{p_canteen_name:name.trim()});if(rpcError)throw rpcError;window.location.reload()}catch(err:any){setError(err?.message||'Could not create your canteen.')}finally{setBusy(false)}};
 return <div className="flex min-h-screen items-center justify-center bg-gray-50 px-4 py-8"><div className="w-full max-w-md rounded-2xl bg-white p-6 shadow-lg sm:p-8"><div className="text-center"><div className="text-2xl font-extrabold text-primary-700">Go Canteen</div><h2 className="mt-3 text-2xl font-extrabold text-gray-900">Create Your Canteen</h2></div><p className="mt-2 text-center text-sm text-gray-600">Enter your canteen name to start managing members, orders, billing and payments.</p><form onSubmit={submit} className="mt-6 space-y-4">{error&&<div className="rounded-md bg-red-100 p-3 text-sm text-red-700">{error}</div>}<label className="block text-sm font-semibold text-gray-700">Canteen Name<input required maxLength={120} value={name} onChange={e=>setName(e.target.value)} className="mt-1 min-h-12 w-full rounded-lg border border-gray-300 px-3 py-3 text-gray-900" placeholder="My Canteen"/></label><button disabled={busy} className="min-h-12 w-full rounded-lg bg-primary-600 px-4 py-3 text-sm font-semibold text-white disabled:opacity-50">{busy?'Creating…':'Create Canteen'}</button></form><button type="button" onClick={()=>void logout()} className="mt-4 w-full text-sm font-semibold text-gray-500">Logout</button></div></div>};
export default CanteenOnboarding;
