import React from 'react';
import { useAuth } from '../../context/AuthContext';
import NotificationBell from '../employee/NotificationBell';
interface HeaderProps { onChangePassword:()=>void; }
const Header:React.FC<HeaderProps>=({onChangePassword})=>{const{user,logout}=useAuth();return <header className="sticky top-0 z-40 w-full overflow-hidden border-b border-gray-200 bg-white shadow-sm"><div className="mx-auto w-full max-w-7xl px-4 pt-[max(10px,env(safe-area-inset-top))] pb-3 sm:px-6 lg:px-8"><div className="flex min-w-0 items-center justify-between gap-3"><div className="min-w-0"><h1 className="truncate text-2xl font-extrabold leading-tight text-primary-700 sm:text-3xl">Go Canteen</h1><p className="text-xs font-medium text-gray-500 sm:text-sm">Canteen Management</p></div><div className="flex min-w-0 items-center gap-2 sm:gap-3"><div className="hidden min-w-0 text-right sm:block"><p className="truncate text-base font-semibold text-gray-700">Welcome, {user?.name||user?.id}</p><p className="text-sm capitalize text-gray-500">{user?.role}</p></div>{user?.role==='employee'&&<NotificationBell/>}{user?.role==='admin'&&<button type="button" onClick={onChangePassword} className="min-h-11 rounded-lg bg-gray-100 px-4 py-2 text-sm font-semibold text-gray-700 hover:bg-gray-200">Change Password</button>}<button type="button" onClick={logout} className="min-h-11 rounded-lg bg-red-600 px-4 py-2 text-sm font-semibold text-white hover:bg-red-700">Logout</button></div></div><div className="mt-2 min-w-0 sm:hidden"><p className="truncate text-sm font-semibold text-gray-700">Welcome, {user?.name||user?.id}</p></div></div></header>};export default Header;
