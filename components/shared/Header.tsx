import React from 'react';
import {useAuth}from'../../context/AuthContext';
import NotificationBell from '../employee/NotificationBell';
interface HeaderProps{onChangePassword:()=>void;onOpenNotifications?:()=>void;onOpenBill?:(billId:string)=>void;}
const LogoutIcon=()=> <svg aria-hidden="true" viewBox="0 0 24 24" className="h-5 w-5 fill-none stroke-current" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"><path d="M10 17l5-5-5-5"/><path d="M15 12H3"/><path d="M21 19V5a2 2 0 0 0-2-2h-6"/></svg>;
const Header:React.FC<HeaderProps>=({onChangePassword,onOpenNotifications,onOpenBill})=>{const{user,logout}=useAuth();const admin=user?.role==='admin';return <header className="sticky top-0 z-40 w-full border-b border-gray-200 bg-white shadow-sm"><div className="mx-auto w-full max-w-7xl px-3 pt-[max(8px,env(safe-area-inset-top))] pb-2 sm:px-6 lg:px-8"><div className="flex min-w-0 items-center justify-between gap-3"><div className="min-w-0"><h1 className={`${admin?'text-xl sm:text-2xl':'text-2xl sm:text-3xl'} truncate font-extrabold leading-tight text-primary-700`}>Go Canteen</h1><p className="text-xs font-medium text-gray-500 sm:text-sm">Canteen Management</p></div><div className="flex min-w-0 items-center gap-1.5 sm:gap-3"><div className="hidden min-w-0 text-right sm:block"><p className="truncate text-base font-semibold text-gray-700">Welcome, {user?.name||user?.id}</p><p className="text-sm capitalize text-gray-500">{user?.role}</p></div>{user?.role==='employee'&&<NotificationBell onViewAll={onOpenNotifications} onOpenBill={onOpenBill}/>} {admin&&<button type="button" onClick={onChangePassword} className="rounded-md px-2.5 py-1.5 text-xs font-semibold text-gray-600 hover:bg-gray-100 sm:px-3 sm:text-sm">Change Password</button>}<button type="button" onClick={logout} aria-label="Logout" title="Logout" className={`${admin?'h-9 w-9 rounded-md px-0 text-base':'min-h-10 rounded-lg px-3 text-sm'} flex shrink-0 items-center justify-center bg-red-600 font-semibold text-white hover:bg-red-700`}>{admin?<LogoutIcon/>:'Logout'}</button></div></div><div className="mt-1 min-w-0 sm:hidden"><p className="truncate text-xs font-semibold text-gray-700">Welcome, {user?.name||user?.id}</p><p className="text-[11px] capitalize text-gray-500">{user?.role}</p></div></div></header>};
export default Header;
